git clone [https://github.com/jmbvcxd/socionics-research/tree/mainrch]
cd socionics-research

# Create a virtual environment (recommended)
python -m venv venv
source venv/bin/activate  

# On Windows, use:
venv\Scripts\activate

# Install dependencies including black, flake 8, and pytest
pip install -r requirements.txt
